/**
 *
 * 1. use form tag and name to get values
 *  Form a access e.target.[name of the input filed].value
 * 2. Controlled element : use individual field state for each input element
 *
 * 3. controlled element : one object holding all the input fields values
 * 4. uncontrolled element : useRef to create a reference to the element and access value by using like: nameRef.current.value
 * 5. use hook to handle states and submit and error. example react hook form
 * 6. Reuseable component
 */
